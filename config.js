//config

//mysql config
if (!config) {
    var config = {
        mysql: {
            host: 'localhost',
            user: 'nodejs',
            password: 'nodejs',
            port: '3306',
            database: 'nodejs'
        }
    }
}





module.exports = config;
